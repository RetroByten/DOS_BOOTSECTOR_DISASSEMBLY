RMPAD.C - Example simple C program source to read in the assembled, patched 
binary built from BOOTSECT.ASM. Written so that it only requires minor syntax
edits to compile with IBMC Compiler 1.0 (which uses an early K&R syntax)

